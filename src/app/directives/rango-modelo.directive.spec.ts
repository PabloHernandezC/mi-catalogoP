import { RangoModeloDirective } from './rango-modelo.directive';

describe('RangoModeloDirective', () => {
  it('should create an instance', () => {
    const directive = new RangoModeloDirective();
    expect(directive).toBeTruthy();
  });
});
